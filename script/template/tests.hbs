module {{moduleName}}Tests exposing (suite)

{-| Unit tests for testing the Bubblegum.Entity.{{moduleName}}

  **generated** in generator/src/generator.ts

-}
import Test exposing (..)
import Expect as Expect
import FunctionTester exposing (ok,ok1,ok2,ok3)
import {{moduleName}}TestData exposing (..)
import {{packageNameDot}}.{{moduleName}} as {{moduleName}}


suite : Test
suite =
    describe "The {{moduleName}} module"
        [
            {{#each functions}}
            describe "{{functionName}}"
            [
               {{#states}}
               {{#has1Param}}
               fuzz fuzzyV1{{firstUpper functionName}} "{{functionName}} should return {{state.text}}" <|
                \v1 ->
                    {{moduleName}}.{{functionName}} (validP1{{firstUpper functionName}}For{{state.camelCaseUpper}} v1)
                    |> summarize{{firstUpper functionName}}For{{state.camelCaseUpper}}
                    |> Expect.equal {{ok}},
               {{/has1Param}}
               {{#has2Params}}
               fuzz2 fuzzyV1{{firstUpper functionName}} fuzzyV2{{firstUpper functionName}} "{{functionName}} should return {{state.text}}" <|
                \v1 v2 ->
                    {{moduleName}}.{{functionName}} (validP1{{firstUpper functionName}}For{{state.camelCaseUpper}} v1) (validP2{{firstUpper functionName}}For{{state.camelCaseUpper}} v2)
                    |> summarize{{firstUpper functionName}}For{{state.camelCaseUpper}}
                    |> Expect.equal {{ok}},
               {{/has2Params}}
               {{#has3Params}}
               fuzz3 fuzzyV1{{firstUpper functionName}} fuzzyV2{{firstUpper functionName}} fuzzyV3{{firstUpper functionName}} "{{functionName}} should return {{state.text}}" <|
                \v1 v2 v3->
                    {{moduleName}}.{{functionName}} (validP1{{name.firstUpper}}For{{state.camelCaseUpper}} v1) (validP2{{firstUpper functionName}}For{{state.camelCaseUpper}} v2) (validP3{{firstUpper functionName}}For{{state.camelCaseUpper}} v3)
                    |> summarize{{firstUpper functionName}}For{{state.camelCaseUpper}}
                    |> Expect.equal {{ok}},
                {{/has3Params}}
                {{/states}}
            ],
            {{/each}}
            
        ]